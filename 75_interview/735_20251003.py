class Solution:
    def asteroidCollision(self, asteroids: List[int]) -> List[int]:
        survived_asteroids = []
        for i in range(len(asteroids)):
            if len(survived_asteroids) == 0:
                pass
            elif asteroids[i] > 0:
                survived_asteroids.append


        